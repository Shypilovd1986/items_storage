language: python
python:
  - "3.9.6"

install:
  - "pip install -r requirements.txt"
  - "pip install coverage"


script:
  - "coverage run -m pytest tests"
  - "pylint main.py"
after_script:
  - "coveralls"

